
# Nothing vendors. The entry that used to sit here described a fallback a
# deleted resolver looked for; keeping it would document an invitation to the
# practice this repository stopped. If a nutshell ends up in the tree by hand,
# that is a thing to notice in `git status`, not a thing to hide.
